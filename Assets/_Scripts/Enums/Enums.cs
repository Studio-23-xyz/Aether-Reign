public enum SpellCastType
{
    PointTarget,
    Projectile,
    AllyTarget,
    EnemyTarget,
    None
}

public enum CellState
{
    Normal,
    Blazed,
    Frosted,
    Shocked,
    Poisoned
}

public enum AIState
{
    Idle,
    Combat
}